<!-- Notification Dropdown -->
<div id="notification-dropdown" class="notification-dropdown">
    <div class="notification-dropdown-header">
        <h3>Notifications</h3>
        <button class="notification-dropdown-close" aria-label="Close notifications">
            <i class="fas fa-times"></i>
        </button>
    </div>
    <div class="notification-dropdown-body">
        <div class="notification-list">
            <!-- Static notification items -->
            <div class="notification-item unread">
                <div class="notification-icon">
                    <i class="fas fa-users"></i>
                </div>
                <div class="notification-content">
                    <div class="notification-title">New Room Invitation</div>
                    <div class="notification-message">You've been invited to join the Netflix Premium room by John Doe</div>
                    <div class="notification-time">2 minutes ago</div>
                </div>
                <div class="notification-actions">
                    <button class="notification-action-btn accept" title="Accept">
                        <i class="fas fa-check"></i>
                    </button>
                    <button class="notification-action-btn decline" title="Decline">
                        <i class="fas fa-times"></i>
                    </button>
                </div>
            </div>

            <div class="notification-item unread">
                <div class="notification-icon">
                    <i class="fas fa-credit-card"></i>
                </div>
                <div class="notification-content">
                    <div class="notification-title">Payment Due</div>
                    <div class="notification-message">Your Spotify Family subscription payment is due in 3 days</div>
                    <div class="notification-time">1 hour ago</div>
                </div>
                <div class="notification-actions">
                    <button class="notification-action-btn primary" title="Pay Now">
                        <i class="fas fa-credit-card"></i>
                    </button>
                </div>
            </div>

            <div class="notification-item">
                <div class="notification-icon">
                    <i class="fas fa-bell"></i>
                </div>
                <div class="notification-content">
                    <div class="notification-title">Room Update</div>
                    <div class="notification-message">The Disney+ room has been updated with new member Sarah</div>
                    <div class="notification-time">3 hours ago</div>
                </div>
            </div>

            <div class="notification-item">
                <div class="notification-icon">
                    <i class="fas fa-gift"></i>
                </div>
                <div class="notification-content">
                    <div class="notification-title">Welcome Bonus</div>
                    <div class="notification-message">You've earned 50 points for joining your first room!</div>
                    <div class="notification-time">1 day ago</div>
                </div>
            </div>

            <div class="notification-item">
                <div class="notification-icon">
                    <i class="fas fa-info-circle"></i>
                </div>
                <div class="notification-content">
                    <div class="notification-title">System Update</div>
                    <div class="notification-message">New features have been added to improve your experience</div>
                    <div class="notification-time">2 days ago</div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
/* Notification Dropdown Styles */
.notification-dropdown {
    position: fixed;
    top: 80px;
    right: 20px;
    width: 380px;
    max-height: 500px;
    background-color: var(--card-bg);
    border: 1px solid var(--divider);
    border-radius: 12px;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.12);
    z-index: 1000;
    opacity: 0;
    visibility: hidden;
    transform: translateY(-10px);
    transition: all 0.3s ease;
    overflow: hidden;
}

.notification-dropdown.active {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
}

.notification-dropdown-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 1rem 1.25rem;
    border-bottom: 1px solid var(--divider);
    background-color: var(--card-bg);
}

.notification-dropdown-header h3 {
    margin: 0;
    font-size: 1rem;
    font-weight: 600;
    color: var(--text-color);
}

.notification-dropdown-close {
    background: none;
    border: none;
    color: var(--secondary-color);
    font-size: 1rem;
    cursor: pointer;
    width: 24px;
    height: 24px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s;
}

.notification-dropdown-close:hover {
    color: var(--primary-color);
    background-color: rgba(84, 97, 255, 0.1);
}

.notification-dropdown-body {
    max-height: 350px;
    overflow-y: auto;
    padding: 0;
}

.notification-list {
    padding: 0;
    margin: 0;
}

.notification-item {
    display: flex;
    align-items: flex-start;
    padding: 0.75rem 1.25rem;
    border-bottom: 1px solid var(--divider);
    transition: background-color 0.3s;
    position: relative;
}

.notification-item:hover {
    background-color: rgba(84, 97, 255, 0.05);
}

.notification-item.unread {
    background-color: rgba(84, 97, 255, 0.08);
}

.notification-item.unread::before {
    content: '';
    position: absolute;
    left: 0;
    top: 0;
    bottom: 0;
    width: 3px;
    background: var(--primary-gradient);
}

.notification-icon {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: var(--primary-gradient);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-right: 0.75rem;
    flex-shrink: 0;
    font-size: 0.8rem;
}

.notification-content {
    flex: 1;
    min-width: 0;
}

.notification-title {
    font-weight: 600;
    color: var(--text-color);
    margin-bottom: 0.2rem;
    font-size: 0.85rem;
}

.notification-message {
    color: var(--secondary-color);
    font-size: 0.75rem;
    line-height: 1.3;
    margin-bottom: 0.3rem;
}

.notification-time {
    color: var(--secondary-color);
    font-size: 0.7rem;
    opacity: 0.8;
}

.notification-actions {
    display: flex;
    gap: 0.3rem;
    margin-left: 0.5rem;
    flex-shrink: 0;
}

.notification-action-btn {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.7rem;
    transition: all 0.3s;
}

.notification-action-btn.accept {
    background-color: var(--success-color);
    color: white;
}

.notification-action-btn.accept:hover {
    background-color: #28a745;
    transform: scale(1.1);
}

.notification-action-btn.decline {
    background-color: var(--danger-color);
    color: white;
}

.notification-action-btn.decline:hover {
    background-color: #dc3545;
    transform: scale(1.1);
}

.notification-action-btn.primary {
    background-color: var(--primary-color);
    color: white;
}

.notification-action-btn.primary:hover {
    background-color: #4a5bff;
    transform: scale(1.1);
}

/* Scrollbar styling for notification list */
.notification-dropdown-body::-webkit-scrollbar {
    width: 4px;
}

.notification-dropdown-body::-webkit-scrollbar-track {
    background: transparent;
}

.notification-dropdown-body::-webkit-scrollbar-thumb {
    background-color: var(--divider);
    border-radius: 2px;
}

.notification-dropdown-body::-webkit-scrollbar-thumb:hover {
    background-color: var(--secondary-color);
}

/* Responsive adjustments */
@media (max-width: 768px) {
    .notification-dropdown {
        position: fixed;
        top: 80px;
        right: 1rem;
        left: 1rem;
        width: auto;
        max-height: 60vh;
    }
    
    .notification-dropdown-header {
        padding: 0.75rem 1rem;
    }
    
    .notification-item {
        padding: 0.6rem 1rem;
    }
    
    .notification-icon {
        width: 28px;
        height: 28px;
        margin-right: 0.6rem;
    }
}

@media (max-width: 480px) {
    .notification-dropdown {
        top: 70px;
        right: 0.5rem;
        left: 0.5rem;
        max-height: 70vh;
    }
    
    .notification-dropdown-header h3 {
        font-size: 0.9rem;
    }
    
    .notification-item {
        padding: 0.5rem 0.75rem;
    }
    
    .notification-icon {
        width: 24px;
        height: 24px;
        margin-right: 0.5rem;
    }
    
    .notification-title {
        font-size: 0.8rem;
    }
    
    .notification-message {
        font-size: 0.7rem;
    }
}
</style>

<script>
// Notification Dropdown JavaScript
document.addEventListener('DOMContentLoaded', function() {
    console.log('Notification dropdown script loaded');
    
    const notificationBtn = document.querySelector('.notification-btn');
    const notificationDropdown = document.getElementById('notification-dropdown');
    const notificationDropdownClose = document.querySelector('.notification-dropdown-close');

    console.log('Notification button found:', !!notificationBtn);
    console.log('Notification dropdown found:', !!notificationDropdown);

    // Open notification dropdown
    if (notificationBtn) {
        notificationBtn.addEventListener('click', function(e) {
            e.preventDefault();
            e.stopPropagation();
            
            console.log('Notification button clicked');
            
            // Close any other open dropdowns
            const allDropdowns = document.querySelectorAll('.notification-dropdown.active');
            allDropdowns.forEach(dropdown => {
                if (dropdown !== notificationDropdown) {
                    dropdown.classList.remove('active');
                }
            });
            
            notificationDropdown.classList.toggle('active');
            console.log('Dropdown active:', notificationDropdown.classList.contains('active'));
        });
    }

    // Close notification dropdown
    function closeNotificationDropdown() {
        notificationDropdown.classList.remove('active');
        console.log('Dropdown closed');
    }

    if (notificationDropdownClose) {
        notificationDropdownClose.addEventListener('click', function(e) {
            e.stopPropagation();
            closeNotificationDropdown();
        });
    }

    // Close dropdown when clicking outside
    document.addEventListener('click', function(e) {
        if (!notificationDropdown.contains(e.target) && !notificationBtn.contains(e.target)) {
            closeNotificationDropdown();
        }
    });

    // Close dropdown on escape key
    document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape' && notificationDropdown.classList.contains('active')) {
            closeNotificationDropdown();
        }
    });

    // Handle notification action buttons
    document.addEventListener('click', function(e) {
        if (e.target.closest('.notification-action-btn')) {
            const btn = e.target.closest('.notification-action-btn');
            const notificationItem = btn.closest('.notification-item');
            
            if (btn.classList.contains('accept')) {
                // Handle accept action
                notificationItem.style.opacity = '0.5';
                setTimeout(() => {
                    notificationItem.remove();
                }, 300);
            } else if (btn.classList.contains('decline')) {
                // Handle decline action
                notificationItem.style.opacity = '0.5';
                setTimeout(() => {
                    notificationItem.remove();
                }, 300);
            } else if (btn.classList.contains('primary')) {
                // Handle primary action (like pay now)
                console.log('Primary action clicked');
            }
        }
    });
});
</script> 